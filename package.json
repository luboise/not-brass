{
	"name": "not-brass",
	"version": "1.0.0",
	"description": "A clone of a non-specified board game.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc -p . -w",
		"start": "nodemon src/index.js",
		"dev": "nodemon src/index.ts"
	},
	"author": "luboise",
	"license": "ISC",
	"dependencies": {
		"nodemon": "^3.0.1"
	}
}
